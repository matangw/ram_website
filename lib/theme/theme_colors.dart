import 'package:flutter/material.dart';

/// Optimistic memorial color palette.
/// Warm cream backgrounds, sage green accents, soft charcoal text.
class ThemeColors {
  ThemeColors._();

  static const Color kSurface = Color(0xFFFAF7F2);
  static const Color kSurfaceSecondary = Color(0xFFF5F0E8);
  static const Color kAccentWarm = Color(0xFF8B7355);
}
